require 'spec_helper'

describe "ElderReportsHerDoings" do
  it "finds a custom she's doing or create it" do
    visit users_path
    # see ppl pushing it everday
    # bubbling up(when you do detail, it bubbles up to abstract layer hack_tags)
    # make best appropriate 3 ppl for every user
    # when everybody done, notify those 3
    # everypage can be bookmarked as a button to do a done action
    # suggest half done / half undone per day to adjust your best appropriate hack_tag set
  end
end
